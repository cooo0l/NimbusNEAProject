/*#include <Arduino.h>

void setup() {

  // Configured the on-board LED to output

  pinMode(LED_BUILTIN, OUTPUT);

}

void loop() {

  // Sets the voltage to 5V (turns it on)
  digitalWrite(LED_BUILTIN, HIGH);  
  // Wait 0.5 seconds
  delay(500);

  // Sets the voltage to 0V (turns it off)
  digitalWrite(LED_BUILTIN, LOW);
  // Wait 0.5 seconds
  delay(500);
}
*/
